/**
 * Canonical site metadata used by usePageMeta, and mirrored in the static
 * tags in index.html, public/robots.txt and public/sitemap.xml.
 * When MakeFrame moves to a custom domain, update SITE_URL here AND in
 * those three static files.
 */
export const SITE_URL = 'https://makeframe-prod.pages.dev'

export const SITE_NAME = 'MakeFrame'

export const DEFAULT_TITLE = 'MakeFrame — Film pre-production, from beat sheet to shot list'

export const DEFAULT_DESCRIPTION =
  'MakeFrame is a film pre-production suite that carries one story from beat sheet to screenplay to storyboard to shot list — in a single tool built for writer-directors.'

/** Public contact for legal pages. Swap for a branded address when one exists. */
export const CONTACT_EMAIL = 'theatulpathak1@gmail.com'
