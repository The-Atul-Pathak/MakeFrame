# Dependencies
# No trailing slash: node_modules is a symlink on this machine (relocated
# outside iCloud-synced Documents to avoid sync-eviction build hangs), and a
# trailing-slash pattern only matches real directories, not symlinks.
node_modules

# Build output
dist/
dist-ssr/

# Vite
*.local
.vite/

# TypeScript
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# iCloud sync duplicates — Documents is iCloud-synced, so conflicted copies land
# as "<name> 2.<ext>" / "<name> 2" next to the original. Never commit these.
* 2
* 2.*

# Editor
.vscode/
.idea/
*.swp
*.swo

# Env
.env
.env.*
.env.local
.env.*.local
!.env.example
*.key
*.pem

# Supabase
supabase/.temp/

# Logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
